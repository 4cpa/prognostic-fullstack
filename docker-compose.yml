version: "3.8"

services:
  backend:
    build: ./backend
    container_name: forecast-backend
    ports:
      - "8000:8000"
    restart: always

  frontend:
    build: ./frontend
    container_name: forecast-frontend
    ports:
      - "3000:3000"
    restart: always
    depends_on:
      - backend
